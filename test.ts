// type annotation
// 특정 타입으로 지정해 주는 것
let a : string;

a = "Mark";
// a = 39;


// Primitive Type : 오브젝트와 레퍼런스 형태가 아닌 실제 값을 저장하는 자료형
// boolean, number, string, symbol, null, undefined
// literal 값 : 값 그 자체를 문자로 값을 할당하는 방식/ 으로 Primitive 타입의 서브 타입을 나타낼 수 있다. true
// 래퍼 객체로 만들 수 있다. new Boolean(false), new String('workd'), new Number(42) => 'object'형
